datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Emails {
  id      String @id @default(uuid())
  name    String
  email   String
  message String
}
